# Distribute Eta Core Decomposition

### Distribute k-core Decomposition

### Core Decomposition in Uncertain Graph

### Distributed $\eta$-core in Probabilistic Graph


run the algorithm

VM options
-Xms4096m -Xmx4096m -Dfile.encoding=UTF-8

maybe bigger